/* ============================================
   FAQ Data — Icon Commerce College
   Thin re-export so consumers can import
   `FAQS` from a stable path. The single source
   of truth is `seoConfig.faqs` so the visible
   FAQ section and the FAQPage JSON-LD schema
   stay in sync.
   ============================================ */

import seoConfig from '../config/seo';

export const FAQS = seoConfig.faqs;
